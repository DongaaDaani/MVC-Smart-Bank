@model IEnumerable<NETBANK.Models.Account>

@{
    ViewBag.Title = "AllAccount";
}

<h2>AllAccount</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CardType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cash)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BankId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CardType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BankId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CardNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.CardNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CardNumber })
        </td>
    </tr>
}

</table>